import styled from 'styled-components/native';
import {RFValue} from 'react-native-responsive-fontsize';


export const WarnText = styled(Text)`
  font-size:   margin-bottom: ${RFPercentage(5)}px;
;

`;

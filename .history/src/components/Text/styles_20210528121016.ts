import styled from 'styled-components/native';
import {Text} from 'react-nativeá;
import {RFValue} from 'react-native-responsive-fontsize';


export const WarnText = styled(Text)`
  font-size:   ${RFValue(16)}px;
  color: ${({}) => theme.}
;

`;

import styled from 'styled-components/native';
import {ScreenMainText} from '../../components/Text';
import {View} from 'react-native';

export const Container = styled(View)``;

export const MainText = styled(Sc)``;
